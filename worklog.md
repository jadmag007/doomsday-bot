# Doomsday Tyranny Bot — Журнал работ

---
Task ID: 1
Agent: main (Super Z)
Task: Автоматизация игры Doomsday Tyranny (Telegram Mini App) на Android/Termux:
ребут производства каждые 12ч (Premium), часовой скан ресурсов с уведомлениями,
веб-панель с таймерами и настройками, автоустановка и автообновление из архива в Загрузках.

Work Log:
- Исследована игра через web-search: бот @DoomsDayTyrannybot, Mini App t.me/DoomsDayTyrannybot/play
- Прочитан официальный справочник (telegra.ph/Doomsday-Tyranny--Alpha-07-02):
  Premium = 12ч пассивного производства; ребут через вкладку «Производство» → текущий цикл → ребут
  (роутеры или память); состояния ресурсов: «Склад переполнен», «Требуется перезагрузка», «Пауза»,
  «Перегрев», «Дефицит» и др.; 40+ ресурсов; бот-чат — /start + уведомления
- Уточнены требования пользователя (AskUserQuestion): пуш-уведомления termux-notification,
  режим по расписанию (cron), скан 1ч, автообновление из Загрузок, все события в отчёты,
  веб-панель с таймерами и всеми настройками
- Решение по архитектуре: Telethon (MTProto) → resolve webview URL (RequestAppWebView) →
  TMA HTTP-API драйвер с настраиваемыми шагами + авто-Discovery эндпоинтов из JS-бандла игры;
  catch-up чтение уведомлений бота в чате; веб-панель (stdlib http) как termux-service;
  cronie каждые 10 мин; termux-notification; апдейтер из /storage/emulated/0/Download

Stage Summary:
- Игра — TMA (Mini App), поэтому автоматизация строится через MTProto webview + HTTP API игры
- Структура проекта: lib/{paths,config,db,notify,tgapi,tma,engine,worker,updater,webui}.py,
  web/{index.html,style.css,app.js}, bin/doomsday, install.sh, update.sh, uninstall.sh,
  boot/, service/, VERSION, requirements.txt, README.md
- В разработке: базовые модули

---
Task ID: 1 (финал)
Agent: main (Super Z)
Task: Сборка, тестирование и упаковка v1.0.0

Work Log:
- Написаны все модули: config (дефолты/миграция/валидация/маскирование), db (SQLite WAL:
  kv/events/runs/resources), notify (termux-notification с кнопкой «Открыть игру»),
  tgapi (Telethon: connect/login/RequestAppWebView/from_bot_menu, чтение чата бота),
  tma (parse webview URL → init_data, HTTP-шаги с {{шаблонами}} и extract, discovery
  эндпоинтов из JS-бандла), engine (scan с порогами 90/100% и дедупликацией уведомлений,
  reboot со страховочным режимом без API, summary, check, discover),
  worker (cron-цикл с троттлингом повторов retry_failed_minutes, selftest, setup-мастер),
  updater (архивы из Загрузок: sha256-дедуп, staging, бэкап, откат, миграция конфига,
  pip при изменении requirements, рестарт сервиса), webui (stdlib HTTP, PIN-auth,
  spawn действий через bin/doomsday, overview/config/events/runs/log API)
- Веб-панель: 5 вкладок (Дашборд с обратным отсчётом, Таймеры, Настройки, API/Discovery
  с редактором HTTP-шагов, Журнал), тёмная тема, mobile-first
- Установщик: pkg, termux-setup-storage, копирование, venv+telethon, setup-мастер,
  Termux:Boot, runit-сервис doomsday-web, crontab */10, симлинк doomsday в $PREFIX/bin
- Исправлены баги в процессе тестирования: PYTHONPATH в bin/doomsday (критично для cron),
  ReferenceError rb в renderOverview, дедуп уведомлений, троттлинг повторов, --id в
  termux-notification, уведомление о провале ребута, WARN_PCT из конфига
- Тесты: py_compile/sh -n/node --check — все ок; selftest 16/16; веб-интеграционный тест
  (все эндпоинты, PUT конфига, 401/404/403, PIN-flow); тест апдейтера (v1.1.0 применён,
  повтор пропущен, конфиг сохранён, exec-биты, бэкап); cron-троттлинг (2 прохода);
  визуальная проверка через agent-browser + VLM — дефектов нет
- Упаковка: download/doomsday-bot-v1.0.0.zip (64.5 КБ, 23 файла, права 755/644),
  download/README-doomsday-bot.md; архив проверен распаковкой + selftest из него

Stage Summary:
- Готов к установке на телефон: unzip → install.sh → login → check → discover
- До настройки TMA-шагов работает «страховочный режим»: вовремя приходит пуш с кнопкой
  «Открыть игру»; discovery-отчёт — основа для v1.1 с точными эндпоинтами (через
  автообновление из Загрузок)
- Ключевой факт для будущих версий: игра @DoomsDayTyrannybot, Mini App short_name "play",
  Premium=12ч, состояния: «Склад переполнен», «Требуется перезагрузка», «Перегрев», «Пауза»

---
Task ID: 3
Agent: main (Super Z)
Task: Диагноз «кнопка собрать всю память — только скан запускает и всё»
и реализация v2.4.4 (действие sell-all).

Work Log:
- Песочница сбросилась до v1.0.0: PAT (.secrets/gh_pat.b64) утерян,
  свежие логи устройства и пуш недоступны до повторной передачи токена.
  Код v2.4.3 восстановлен из /tmp/my-project/doomsday-bot (снапшот).
- Диагноз БЕЗ логов (подтверждён локальной репродукцией _exchange_plan):
  кнопка «Обменять сейчас» продаёт только по правилам; склады пользователя
  кассета 79% / HDD 67% / дискета 17% при пороге 90% → план пуст →
  «только скан». Ошибок sellItem нет (не было бы пуша «Автообмен не удался»).
  Единственное, что продавалось по правилам — uran_pills (mode always).
- v2.4.4 — действие «Собрать всю память» (sell-all), 8 файлов:
  * engine._exchange_plan(cfg, res, force_all): форс-план из ВСЕХ байтовых
    носителей (cassete/floppy/hdd, amount=count, keep=0, mode="manual"),
    мимо порогов/правил/переключателя exchange.enabled; DDT-ресурсы
    (uran_pills/u235/ddt_res) в форс-план НЕ входят;
  * engine._auto_exchange(..., force_all): подпись режима «вручную (вся
    память)», push «Память собрана (вручную)» (шлётся даже при выключенном
    exchange_report — ручное действие);
  * engine.action_scan(cfg, sell_all=False): форс-продажа после скана,
    result["sell_all"] маркер;
  * worker.cmd_sell_all + CLI «doomsday sell-all» + wait_lock;
  * webui: sell-all в allowed действиях;
  * web: золотая кнопка ⇩ «Собрать всю память» на дашборде и в карточке
    обмена, confirm() перед запуском, старая переименована в «Обмен по
    правилам», тосты с человеческими названиями действий, .btn.gold стиль;
  * README: команда + changelog 2.4.4; VERSION 2.4.4.
- Тесты: test_auto_exchange +3 блока force_all (10 проверок: мимо
  выключателя/порогов, DDT не тронут, keep=0, пустые склады, чистая
  функция); selftest 95→100 (5 проверок sell-all); test_v244_smoke.sh
  НОВЫЙ (кнопки в разметке ×2, API 200/400, CLI, selftest); v242-smoke
  зелёный; reboot_session/logs_push/starter зелёные; pyflakes чист.
- Визуально (agent-browser 412px + VLM): кнопки на месте, золото
  применяется, confirm блокирует запрос (отмена → 0 запросов, принятие →
  1 + тост «Сбор всей памяти запущен»), дефектов вёрстки нет.
- Патч против чистой v2.4.3: /tmp/v244.patch (8 файлов). Для пуша и разбора
  свежих логов нужен PAT от пользователя (песочница сбросилась).

Stage Summary:
- v2.4.4 готова локально, ждёт PAT: клон → patch → selftest → пуш.
- Кнопка «Собрать всю память» продаёт все носители немедленно; «Обмен по
  правилам» — прежнее поведение. DDT — только по правилам.
- Открытые: свежие логи на logs-ветке не разобраны (нет PAT) — после
  передачи токена сверить, нет ли там ещё проблем помимо пустого плана.

---
Task ID: 4
Agent: main (Super Z)
Task: PAT вне песочницы + реорганизация конвейера логов («просмотренные
путаются со свежими») + пуш v2.4.4 (sell-all + гигиена логов).

Work Log:
- PAT сохранён вне песочницы: /home/z/.config/doomsday-bot/gh.pat (600)
  + копия /tmp/doomsday-gh.pat. fetch_device_logs.sh теперь сам находит
  токен по цепочке env GITHUB_PAT → config → /tmp (аргумент не нужен).
- Разобран свежий бандл 23:18 (v2.4.3, единственный в logsbuf/):
  * жалоба «кнопка — только скан» ПОДТВЕРЖДЕНА: run-7/8/9 (23:09–23:10)
    = нажатия кнопки, каждая закончилась «По правилам обмена продавать
    нечего» (склады: кассета 79% / HDD 66.7% / дискета 16.9% < порога 90%);
    ошибок sellItem нет; единственное, что продавалось — uran_pills (always).
    Лечится sell-all из v2.4.4 (см. Task 3).
  * прочее в бандле: ребут 18:37 упал 403 Expired session, ретрай 19:15
    успешен (farm_ends_at обновлён, статусы «требуется ребут» сняты — 0
    вхождений в run-5/6); 20:05 scan+summary штатно; «вечный нужен ребут»
    не воспроизводится.
- КОРЕНЬ «старых логов»: пуш 20:08 ушёл с v2.4.2 — БЕЗ обрезки вовсе
  (обрезка появилась в v2.4.3), поэтому бандл 23:18 продублировал записи
  всего дня (бандлы 20:08 и 23:18 идентичны: run-1 14:19, cron с 12:48).
  В v2.4.3 обрезка была хрупкой: окно «моложе 5 минут не трогать»
  пропускало cron.log после свежего скана и web.log после рестарта.
- v2.4.4 гигиена логов (lib/logspush.py):
  * build_bundle снимает снапшот (inode+размер) каждого лога ДО чтения
    → manifest["log_state"];
  * _trim_consumed(): после успешного пуша отрезает РОВНО отправленное
    (seek snap_size, остаток дозаписанного — сохранить, rewrite+truncate);
    статусы trimmed/rotated/shrunk/gone/error; ротация (иной inode) и
    усохшие файлы не трогаются — уйдут следующим бандлом; никаких
    временных окон; гонка «писатель дописал после сборки» без потерь;
  * _clear_pushed_logs(): обрезка по снапшотам; пустые ротационные
    архивы (*.log.1) и отправленные отчёты удаляются; прун run-логов 20.
- Песочница: fetch_device_logs.sh переписан — подкоманда `done [имя]`:
  клон logs → git mv logsbuf/<b> → archive/<b> → прун архива до 6 →
  commit+push; после разбора ОДНА команда закрывает бандл. Fetch-режим
  в конце напоминает команду. Пуш — явным URL с PAT (origin чистый).
- Тесты: selftest 100→106 (trim: 5 юнит-проверок _trim_consumed + 3
  интеграционных _clear_pushed_logs + манифест-без-снапшота); интеграционный
  test_logs_push.py: push-2 БЕЗ состаривания mtime (главный сценарий бага:
  свежий лог обрезается сразу), новый push-3 «гонка писатель/пуш» (дописанное
  после сборки сохраняется), push-4 сеть-падение; автообмен + smoke v2.4.4
  зелёные; py_compile чист.
- ПУШ v2.4.4 в main: клон v2.4.3 (afd46fb) → точечно 9 файлов (VERSION,
  README, engine, logspush, worker, webui, app.js, index.html, style.css)
  с точными режимами HEAD → selftest из клона зелёный → коммит 635a9c0 →
  push. ВАЖНО на будущее: в песочнице остались legacy lib/updater.py и
  update.sh (удалены из дистрибутива ещё в v2.0.0 «только git-обновления»)
  — при пуше НЕ накатывать rsync'ом всё дерево, только целевые файлы.
- Отработан новый цикл на реальном репо: `fetch_device_logs.sh done`
  заархивировал bundle-20260915-231839 (коммит 17984e0 ветки logs);
  logsbuf/ пуст, archive/ 5 бандлов.

Stage Summary:
- v2.4.4 на main (635a9c0): кнопка «Собрать всю память» (sell-all) +
  точная обрезка логов по снапшоту. Устройство подхватит по git-update.
- Конвейер логов: устройство после пуша оставляет только новое; песочница
  одной командой `done` архивирует разобранное; в logsbuf/ всегда только
  свежее. PAT живёт вне песочницы и подхватывается автоматически.
- Открытые (из backlog): живой тик таймера 1с, статусбар, ночной режим
  (частично есть), подраздел лога автообменов на главной.

---
Task ID: 5
Agent: main (Super Z)
Task: Диагноз «опять ребут не сработал» по свежим логам + таймер
ежедневного бонуса в статусбаре + редизайн DDT-чипа («голая иконка +
+N time») + время ребута в игровом формате. Итог — v2.5.0 (запушена).

Work Log:
- Песочница снова сбросилась: /home/z/my-project/doomsday-bot больше не
  git-репо (git-команды видят репо песочницы), но дерево v2.4.4 цело;
  PAT выжил вне песочницы (gh.pat). Скрипты scripts/ уцелели целиком.
- Свежий бандл 07:12 (v2.4.4, 635a9c0) почти ПУСТ: bot.log 0 б, run-*
  0 б, cron.log = 1 строка «05:54:49 ночной режим — плановые действия
  пропущены». Корень: «ночной пропуск» логируется 1 раз/сутки (dedup по
  дате), тишина остальное время — проходов НЕ БЫЛО вовсе. Единственный
  ночной проход в 05:54 (первый после полуночи!) = Termux был заморожен
  Android'ом с вечера до 05:54 и снова после. Окно авто-ребута 07:04±3
  (цикл до 07:15, reboot_before_end 10 мин) проспано; на момент 07:20
  (скрин игры: «Завершено! Статус: 503») ребута не было. Catch-up в
  _reboot_due_cycle ЕСТЬ (окно открыто/истекло → due) — не хватало
  самого прохода. Wake-lock продлевается каждым проходом, но заморозку
  Android 16 он не всегда побеждает.
- ФИКС (главный): резервный cron-планировщик в процессе веб-панели
  (lib/webui.py): поток каждые 60 с проверяет last_cron_ts; простой
  >660 с → Popen("sh bin/doomsday cron", detached). Дублей нет
  (SingleInstance неблокирующая), ночной режим решает сам cron, событие
  «Резервный проход cron» не чаще 1/час. Функциональный тест: протухший
  last_cron_ts → через ~62 с обновился + событие (scripts/
  test_v250_fallback.sh). Отключается web.cron_fallback=false.
- Цепочка ежедневных наград из КОДА ИГРЫ: скачан JS-бандл с
  telegram-miracle-f1779.web.app (хостинг из discovery-отчёта в архиве
  логов), чанки daily-DoG6l0eh.js + use-is-claimed-today: протокол
  35 дней, награды data/ddt/free_core/cpu|gpu|ram|hpc|quant token,
  количество = count[clamp(LVL,1,len)-1] (state.LVL игрока!), новый
  день в ПОЛНОЧЬ МСК (фикс +3ч к UTC, не таймзона устройства), гэп ≥2
  дней → сброс на день 1, 35-й → 1-й. День 34 = quant_token. Полная
  таблица DAILY_CHAIN в lib/game_data.py (35 дней, counts по уровням).
- engine: _store_daily_state (currentDay/LVL/lastClaimedDate из
  initUser) в action_scan и _refresh_after_reboot; kv daily_state.
- webui: _daily_info() в overview — reset_in_sec (до полуночи МСК),
  claimed_today (логика игры), next_day + next_reward + короткая
  подпись («512 КиБ»/«12 DDT»/«x6»). msk_day_index/
  next_msk_midnight_ms — чистые функции (формат игры).
- Статусбар (web): НОВЫЙ чип ребута в игровом формате — fmtGame
  «ЧЧ:ММ:СС» (formatters.f игры: [ч,м,с]→pad2→join ":"), при открытом
  окне золотое «ребут…», при истёкшем цикле красное мигающее
  «Завершено!» (тайтл: Статус 503, нужен ребут). НОВЫЙ чип ежедневки:
  иконка-подарок + таймер до полуночи МСК + «+N награда», золотая
  подсветка если сегодняшний не собран. DDT-чип переделан: sb-bare —
  маленькая иконка на ПРОЗРАЧНОМ фоне (без плашки) + «+N time»:
  N=склад (продаст ближайший скан, время=до скана) либо N=min (время=
  ETA единицы по доходу входов), нет дохода — приглушённое «+0».
  Имя/счётчик 0/24 ушли в тайтл. Тикеры 1 с обновляют все 4 значения.
- Пойман и починен краш-риск: _daily_info делал json.loads() от kv,
  который при хранении dict (как пишет kv_set) отдал бы TypeError →
  500 всего overview. Теперь принимает dict|str; ddt_eta аналогично
  защищён от двойного кодирования.
- logspush: пустые после обрезки run-*.log и *.log.N удаляются (не
  копятся нулевые run-1…run-9, как в бандле 07:12).
- Тесты: selftest 106→120 (+14: цепочка/уровни/wrap/форматы, daily-
  стейт, полночь МСК 3 шт, резерв 3 шт); test_v250_smoke.sh НОВЫЙ
  (15 проверок: разметка, стили, API daily, движок, selftest);
  test_v250_fallback.sh НОВЫЙ (живой запуск резерва); test_webui.sh
  починен (ждал старую форму timers.reboot_in_sec) + daily-ассерты;
  визуально agent-browser 412px + VLM: все 5 чипов, «+1 00:44:10»,
  красное «Завершено!», золотой бонус, без наездов. Регресс: exchange/
  logspush/v244/webui — зелёные.
- ПУШ v2.5.0: клон 635a9c0 → 10 файлов (VERSION, README, game_data,
  engine, webui, worker, logspush, index.html, app.js, style.css) →
  selftest из клона ✔ → 4512962 → main. Бандл 07:12 заархивирован
  (logsbuf пуст).

Stage Summary:
- v2.5.0 на main (4512962). Ребут больше не зависит от одного cronie:
  панель сама догоняет пропущенные действия при простое >11 мин
  (типовой случай: Android заморозил Termux на ночь).
- Статусбар: [ребут ЧЧ:ММ:СС / Завершено!] [скан] [бонус до полуночи
  МСК +N] [иконка +N time] [ночь].
- Ежедневка: цепочка 35 дней в game_data, скан тащит currentDay/LVL/
  lastClaimedDate из initUser; завтрашняя награда считается по уровню.
- Android всё ещё может заморозить Termux целиком (панель тоже спит) —
  это лечится только настройками телефона (deepsleep/батарея), бот
  теперь сам догоняет при первой оттайке.

---
Task ID: pat-infra
Agent: main (Super Z)
Task: Персистентное хранилище GitHub PAT — пользователь в N-й раз прислал
токен заново, старые места (/home/z/.config, /tmp) не переживают рестарт
песочницы.

Work Log:
- Диагноз: выживает только /home/z/my-project; .config и /tmp — нет.
- ГЛАВНОЕ хранилище: /home/z/my-project/.secrets/gh.pat (600),
  каталог .secrets/ + *.pat добавлены в .gitignore my-project.
- Вычищен gh.pat из git-индекса и истории my-project (git rm --cached,
  amend HEAD, reflog expire + gc --prune=now). Репо без remote — утечки
  не было, теперь невозможна.
- Новый scripts/pat.sh: save/ensure/status/print; ensure — самолечение
  (находит токен в любом из 3 мест и достилает во все остальные).
- fetch_device_logs.sh переведён на pat.sh (цепочка env → .secrets →
  .config → /tmp), самолечение перед каждым.fetch.
- Проверено: 3 копии на месте (600), синтаксис ок, скан утечек по обоим
  репо чистый, токен нигде в трекаемых файлах нет.

Stage Summary:
- ПРАВИЛО НА БУДУЩЕЕ: PAT живёт в /home/z/my-project/.secrets/gh.pat —
  это персистентно, БОЛЬШЕ НЕ ПРОСИТЬ У ПОЛЬЗОВАТЕЛЯ. Восстановление:
  bash scripts/pat.sh ensure. Если вдруг пусто везде — только тогда
  pat.sh save <token>. Значение токена в worklog НЕ писать никогда.

---
Task ID: v2.5.1-release
Agent: main (Super Z)
Task: v2.5.1 — фикс ребута по концу цикла (диагностика бандла 20:55) +
релиз на GitHub. Запрос: «ребут опять не сработал по расписанию».

Work Log:
- Разобран бандл bundle-20260916-205524 (v2.5.0, 4512962): cronie на
  устройстве почти мёртв — 3 прохода за 13.5 ч (08:44/12:51/16:38),
  Android морозил Termux; сканы весь день таскал резервный планировщик
  панели (его stdout шёл в /dev/null — потому в cron.log их не видно).
- Цепочка вечера: цикл истёк 20:44 → в 20:33 юзер жал «Ребут» вручную
  (корректный skip «цикл свежий», до конца 11 мин) → 20:48 скан видел
  «требуется ребут», но ребут — отдельное действие → 20:54:53 ребут
  прошёл (опоздание 10 мин: 11-мин сетка резерва + 20-мин троттлинг
  ошибок в _reboot_due_cycle).
- Фиксы: (1) webui._cycle_end_watch_due — дожим: за 2 мин до конца и
  после истечения (до 30 мин, пока last_reboot_ts <= конца) проход
  запускается сразу, цикл проверки 60→30 с; (2) при истёкшем цикле
  троттлинг ошибок ребута 20 мин → 2 мин; (3) резервные проходы пишут
  план в cron.log с меткой [fallback] (env DOOMSDAY_CRON_FALLBACK);
  (4) анти-шторм спавнов — не чаще 1/мин (kv last_cron_fallback_spawn).
- Тесты: новый scripts/test_v251_smoke.sh (23 проверки — все ок),
  selftest воркера с новыми проверками, регрессии v242/v244/webui/
  v250_fallback — зелёные. Смок v250 устарел (проверял убранный чип
  ребута) — заменён v251.
- ОШИБКА В РЕЛИЗЕ ИСПРАВЛЕНА: в первый push (26523ad) по ошибке вошли
  lib/updater.py + update.sh — мёртвые остатки zip-обновлятора v1.x
  (удалён в v2.0.0, git-only), ссылались на несуществующие
  paths.CODE_ENTRIES. Вычищены fixup-коммитом 4a7ce0b; README-пункт
  про них убран. В dev-дереве песочницы их остатки тоже удалены.
- СБРОС ПЕСОЧНИЦЫ: между командами дерево откатилось к последнему
  автосейву (README-правка пропала). Вывод: после каждого крупного
  шага — git commit в репо песочницы. Dev-дерево пересинхронено из
  клона (источник истины) после fixup.
- Пат-инфра: gh.pat теперь ТРЕКАЕТСЯ локальным git-репо песочницы
  (без remote — наружу не уходит), сбросы больше его не стирают;
  в GitHub-репо не попадает никогда (скан перед пушем).

Stage Summary:
- GitHub main = 4a7ce0b «v2.5.1» (26523ad + fixup). Устройству:
  doomsday start (или git pull + web-restart) — панель сама подхватит.
- Ребут больше не зависит ни от cronie, ни от 11-минутной сетки
  резерва: конец цикла ловится в течение ~30-60 с, ошибки ретраятся
  через 2 мин. cron.log теперь виден весь (метка [fallback]).
- Бандл 20:55 заархивирован (logsbuf пуст).

---
Task ID: full-archive-20260917
Agent: main (Super Z)
Task: Собрать все файлы проекта в один архив (включая личные) для
параллельной работы с другим агентом.

Work Log:
- Проверен статус v2.5.1: на GitHub main = 4a7ce0b (релиз залит ранее).
- Написан PROJECT-MAP.md — карта проекта для другого агента (что где
  лежит, процесс релиза, грабли, источник истины).
- Собран download/doomsday-project-full-20260917.zip (32 МБ, 1555
  файлов): doomsday-bot/ (dev-дерево), корневой .git (вся локальная
  история песочницы), .mainwork/repo (клон GitHub), scripts/, research/,
  upload/ (личные скриншоты), download/ (zip v1.x), tool-results/,
  .logswork/.logspush-test/.starter-test/, .secrets/gh.pat (личное),
  .env, worklog.md, MANIFEST.md, PROJECT-MAP.md. skills/ исключён.
- Проверено: целостность unzip -t OK; remote в клоне чистый (PAT в URL
  не вшит); у doomsday-bot/ собственного .git нет — его история это
  корневой репо песочницы, он в архиве.

Stage Summary:
- Архив: download/doomsday-project-full-20260917.zip — полный снапшот
  проекта на 2026-09-17, v2.5.1, с PAT внутри (.secrets/gh.pat) —
  передавать только по доверенным каналам.

---
Task ID: workspace-reviziya-20260917
Agent: main (Super Z)
Task: Ревизия workspace перед выгрузкой на GitHub (просьба владельца:
удалить старые/неактуальные скрипты, объединить дубли, полное ревью).

Work Log:
- Инвентаризация scripts/ (102 файла): каждый проверен по шапке,
  перекрёстным ссылкам и worklog; проверено самовосстановление фикстур
  test_logs_push/test_starter (setup() пересоздают их с нуля).
- УДАЛЕНО (82 файла): zip-эпоха (build_doomsday_zip.sh, fake_stage/,
  fake_download/, test_updater*.sh/py, патч и скрипты релизов push_130/
  push_200 — в них же вычищен захардкоженный PAT!); устаревшие смоки
  v200/v210/v220/v230/v240/v250 (заменены v251 + регрессии v242/v244/
  v250_fallback); vis*-скрипты (5); разовые артефакты: ui*.png (31),
  vlm*.json (20), search*.json (7), сохранённые html (8), guide.txt,
  v244.patch.
- УДАЛЕНО вне scripts/: tool-results/ (8 старых чтений), download/
  v1.x-zip (9) + старый README, .logswork/err*.txt + vlm-обрезки,
  .logspush-test/ и .starter-test/ (самовосстанавливающиеся фикстуры),
  дубли research/ (44 js идентичны chunks/), doomsday-bot/: *.relbak,
  worker.lock, logs/, мёртвый update.sh (в main его нет с 4a7ce0b).
- ОБЪЕДИНЕНО: все релизные скрипты -> параметризованный release.sh
  <версия> "<описание>" (добавлены синк удалений и скан утечки PAT,
  которых не было в release_251.sh). push_workspace_branch.sh чинен:
  rsync-баг с конечными слэшами вываливал содержимое папок в корень
  (первая версия ветки была кривой, 619 файлов-каша).
- Тесты после чистки: все 8 живых зелёные (v251, v242, v244,
  v250_fallback, webui, logs_push, starter, + selftest воркера).
- Документация: PROJECT-MAP.md переписан (структура, состав scripts/,
  процесс релиза, грабли), MANIFEST.md обновлён.
- Ветка workspace пересоздана force-push: 6f701f7, 239 файлов,
  структура: код бота (из main) + research/ + scripts/ + upload/ +
  logs-archive/ + 3 док-файла. Скан утечек PAT и паттерна github_pat_*
  чистый.

Stage Summary:
- GitHub: main = 4a7ce0b (не тронут), workspace = 6f701f7 — чистое
  рабочее окружение для любого агента с PAT.
- Один релизный скрипт release.sh, один скрипт синка
  push_workspace_branch.sh. Правило rsync зафиксировано в граблях.
- В песочнице scripts/ = 20 живых файлов, research/ без дублей.

---
Task ID: onboarding-doc-20260917
Agent: main (Super Z)
Task: Объяснить владельцу механику передачи проекта между агентами;
закрепить её документом в репо.

Work Log:
- Написан AGENT-ONBOARDING.md: готовый copy-paste промпт для нового
  агента (клон workspace-ветки, порядок чтения доков, формат коммитов,
  worklog-протокол, скан PAT, команда релиза в main), шпаргалка команд,
  инструкции владельцу (как забрать работу агента, гигиена токенов).
- push_workspace_branch.sh: AGENT-ONBOARDING.md добавлен в копируемые
  док-файлы.

Stage Summary:
- Онбординг нового агента = PAT + ссылка на AGENT-ONBOARDING.md в репо.

---
Task ID: onboard-20260918
Agent: main (Super Z, новая сессия)
Task: Онбординг по AGENT-ONBOARDING.md — клон ветки workspace, чтение
документации, инвентаризация состояния проекта перед приёмом задачи владельца.

Work Log:
- Клонирована ветка workspace (037382a) в свежую песочницу
  (/home/z/my-project/project), remote очищен от PAT (гигиена п.1).
- Прочитаны по порядку: PROJECT-MAP.md, worklog.md (вся история),
  MANIFEST.md, AGENT-ONBOARDING.md.
- Сверка состояния: VERSION=2.5.1; main=4a7ce0b (v2.5.1+fixup); код бота
  в workspace идентичен main (152 файла в diff — только research/, scripts/,
  upload/, logs-archive/ и доки); config.json не разошёлся; дерево чистое.
- Инфраструктура (ВАЖНО для будущих релизов): release.sh и
  push_workspace_branch.sh зашиты под СТАРУЮ раскладку песочницы
  (/home/z/my-project/doomsday-bot + .mainwork/...), в новом клоне
  (код в корне workspace) неприменимы — релиз и пуш по ручному потоку из
  AGENT-ONBOARDING.md (rel-ветка + пуш PAT-URL). release.sh остаётся
  справочником (шаги selftest, скан PAT).
- PAT живёт в env GH_PAT шелл-сессии (env НЕ переживает между вызовами
  shell — экспорт в каждой команде); в файлы репо не пишется; перед каждым
  пушем — grep-скан строки токена.
- Осмотрена ветка logs: ждёт детального разбора свежий
  bundle-20260918-101529 (v2.5.1, 4a7ce0b, src_dirty=false). Быстрый
  триаж: ребут сегодня 09:38:33 прошёл через резервный планировщик
  (план ['reboot','scan'], run-66/67 ok) — дожим конца цикла v2.5.1
  сработал в проде; сканы 07:06/07:50/08:45/09:38/10:08 без ошибок;
  вчера 17.09 — разовый 403 PERMISSION_DENIED на initUser (14:03) и серия
  сетевых таймаутов Telegram (16:51, связь восстановилась); cronie на
  устройстве мёртв, нагрузку несёт резервный планировщик панели
  (~1 [fallback]-проход/мин — по дизайну v2.5.x).
- Эта запись + пуш в workspace = проверка полного цикла из новой песочницы.

Stage Summary:
- Рабочая среда готова, задачи владельца принимаются.
- Открытое: бандл 2026-09-18 10:15 не разобран детально и не заархивирован
  (жалоб в логах не видно; протокол — после разбора: git mv
  logsbuf/<бандл> archive/<бандл> на ветке logs + пуш).
- Релиз из этой песочницы — только ручным потоком из AGENT-ONBOARDING.md
  (скрипты старой раскладки несовместимы).
